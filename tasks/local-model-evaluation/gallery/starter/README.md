# Gallery starter

This is a deliberately minimal fixed workspace. The supplied images and
`images.json` manifest are placed in `public/`. Implement the application in
this worktree and use the existing npm scripts to build and test it.

The task contract is in `TASK.md`; its Playwright contract is in
`tests/gallery.spec.ts`. Do not edit those task files.
